package Input;

import java.util.ArrayList;
import java.util.List;

public enum Commands {

    HELLO, EXIT, STATUS, MAKE_COFFEE;

    public String toString(Commands command) {
        return "" + command;
    }
}
